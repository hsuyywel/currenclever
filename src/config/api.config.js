const prod_base = 'https://currenclever-api.fly.dev/api'
const local_base = 'http://localhost:8000/api'

const prod_model = 'https://currenclever-model.fly.dev'
const local_model = 'http://127.0.0.1:5000'

const BASE_URL = prod_base
const MODEL_URL = prod_model

export {
  BASE_URL,
  MODEL_URL
}